/* ==========================================================================
 * 部署配置
 * --------------------------------------------------------------------------
 * 1) 后端 API 基地址（API_BASE）
 *    留空 '' 表示「与前端同源」：
 *      推荐把整个 workbench 目录部署到同一个 Node 服务（前端 + /api 同一域名），
 *      无需任何配置，开箱即用、数据真正共享。
 *    若前后端分离部署（前端用 CloudStudio 静态、后端用 Render / 自有服务器等 Node 主机），
 *      把下面改成后端公网地址，例如：window.API_BASE = 'https://zwcs.onrender.com';
 *
 * 2) 离线模式（OFFLINE_MODE）
 *    false：自动探测后端（默认，用于同进程托管或分离托管的生产环境）。
 *    true ：强制纯前端、不连任何后端（用于 CloudStudio 静态托管等无后端场景，
 *           数据仅存当前设备浏览器，各设备不共享）。
 * ========================================================================== */
window.API_BASE = '';
window.OFFLINE_MODE = false;
